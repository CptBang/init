#Use the output of the previous command (traceroute) to find the name and IP address of the device that makes the link between you (local network) and the outside world
#Following code lists the modems IP address 
traceroute slash16.org | head -2 | tail -1
